version: '3.9'

services:
  server:
    build: .
    container_name: kubernetes-cloud-backend-container
    env_file: .env
    image: kubernetes-cloud-backend-image:latest
    restart: always
    ports:
      - $PORT:$PORT
    # volumes:
    #   - 'server-volume:/server'
# volumes:
#   server-volume:
